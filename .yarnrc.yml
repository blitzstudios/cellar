# A plain `node_modules` tree, matching the app that consumes this package.
nodeLinker: node-modules

# `react-native-nitro-sqlite` ships its build committed, so no install needs to run a script.
enableScripts: false

approvedGitRepositories:
  - "https://github.com/blitzstudios/*"
